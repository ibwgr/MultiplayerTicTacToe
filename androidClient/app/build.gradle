apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    defaultConfig {
        applicationId "ch.ibw.semesterarbeit2017.multiplayertictactoe.multiplayertictactoe"
        minSdkVersion 16
        targetSdkVersion 25
        versionCode 104
        versionName "1.04"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    // rka...
    testOptions {
        unitTests.returnDefaultValues = true
    }

    // rka, wegen Travis bzw.   ./gradlew build connectedCheck
    lintOptions {
        abortOnError false
    }


}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    androidTestCompile('com.android.support.test.espresso:espresso-core:2.2.2', {
        exclude group: 'com.android.support', module: 'support-annotations'
    })
    compile 'com.android.support:appcompat-v7:25.3.1'
    compile 'com.android.support.constraint:constraint-layout:1.0.2'

    // rka: fuer MultiplayerTicTacToe
    compile 'com.github.nkzawa:socket.io-client:0.3.0'
    compile ('io.socket:socket.io-client:0.8.3') {
        // excluding org.json which is provided by Android
        exclude group: 'org.json', module: 'json'
    }

    // rka: fuer StringUtils
    // https://mvnrepository.com/artifact/org.apache.commons/commons-lang3
    //compile group: 'org.apache.commons', name: 'commons-lang3', version: '3.0'


    testCompile 'junit:junit:4.12'
    testCompile "org.mockito:mockito-core:1.+"
    testCompile "org.json:json:20170516"


    // https://mvnrepository.com/artifact/org.powermock/powermock-api-mockito
    //testCompile group: 'org.powermock', name: 'powermock-api-mockito', version: '1.7.0'
    // https://mvnrepository.com/artifact/org.powermock/powermock-mockito-release-full
    //testCompile ('org.powermock:powermock-module-junit4:1.6.2') {
    //    exclude module: 'hamcrest-core'
    //    exclude module: 'objenesis'
    //}
}
